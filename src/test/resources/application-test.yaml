spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kinexys
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create-drop

  test:
    database:
      replace: none